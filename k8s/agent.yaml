# ---------- Multi-Agent System ----------
apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-config
  namespace: ai-agent
data:
  OLLAMA_URL: "http://ollama:11434"
  QDRANT_URL: "http://qdrant:6333"
  SEARXNG_URL: "http://searxng:8080"
  EMBEDDING_MODEL: "nomic-embed-text"
  INTERPRETER_MODEL: "qwen2.5:7b"
  ORCHESTRATOR_MODEL: "qwen3:14b-agent"
  RESEARCH_MODEL: "qwen2.5:7b"
  SYNTHESIS_MODEL: "qwen3:14b-agent"
  CRITIC_MODEL: "qwen2.5:7b"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent
  namespace: ai-agent
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: agent
  template:
    metadata:
      labels:
        app: agent
    spec:
      containers:
        - name: agent
          image: agent:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: agent-config
          resources:
            requests:
              memory: 256Mi
              cpu: 250m
            limits:
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: agent
  namespace: ai-agent
spec:
  selector:
    app: agent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 31400
  type: NodePort
